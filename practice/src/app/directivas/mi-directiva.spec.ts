import { MiDirectiva } from './mi-directiva';

describe('MiDirectiva', () => {
  it('should create an instance', () => {
    const directive = new MiDirectiva();
    expect(directive).toBeTruthy();
  });
});
